<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Add New Proverb</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h2 class="mb-4">Add a New Afghan Proverb</h2>

      <form
        action="/add"
        method="POST"
        class="border p-4 rounded bg-light shadow-sm"
      >
        <div class="mb-3">
          <label for="textDari" class="form-label">Text (Dari)</label>
          <input
            type="text"
            class="form-control"
            id="textDari"
            name="textDari"
            required
          />
        </div>

        <div class="mb-3">
          <label for="textPashto" class="form-label">Text (Pashto)</label>
          <input
            type="text"
            class="form-control"
            id="textPashto"
            name="textPashto"
            required
          />
        </div>

        <div class="mb-3">
          <label for="translationEn" class="form-label"
            >Translation (English)</label
          >
          <input
            type="text"
            class="form-control"
            id="translationEn"
            name="translationEn"
            required
          />
        </div>

        <div class="mb-3">
          <label for="meaning" class="form-label">Meaning</label>
          <textarea
            class="form-control"
            id="meaning"
            name="meaning"
            rows="3"
            required
          ></textarea>
        </div>

        <div class="mb-3">
          <label for="category" class="form-label">Category</label>
          <select class="form-select" id="category" name="category" required>
            <option value="">-- Select Category --</option>
            <option value="wisdom">Wisdom</option>
            <option value="friendship">Friendship</option>
            <option value="patience">Patience</option>
            <option value="humor">Humor</option>
            <!-- Add more categories as needed -->
          </select>
        </div>

        <div class="d-flex justify-content-between">
          <a href="/" class="btn btn-secondary">Cancel</a>
          <button type="submit" class="btn btn-primary">Add Proverb</button>
        </div>
      </form>
    </div>
  </body>
</html>
